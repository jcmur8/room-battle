import{el,button}from'../ui.js';import{t}from'../i18n.js';
export function renderHome(root,data,actions){const active=data.activeSession&&data.activeSession.status!=='complete';const heroes=el('div',{class:'grid two'},...data.children.filter(c=>c.active!==false).map(c=>el('div',{class:'hero-chip'},el('span',{class:'hero-avatar',text:c.avatar}),el('div',{},el('strong',{text:c.displayName}),el('div',{text:c.heroTitle||t('hero')})))));root.replaceChildren(el('section',{class:'child-screen'},el('div',{class:'card hero-card'},el('div',{},el('div',{class:'monster messy',text:active?'😮':'👾'}),el('p',{style:'text-align:center',text:active?t('monsterActive'):t('monsterIdle')})),el('div',{},el('h1',{text:active?t('battleProgress'):t('heroesNeed')}),heroes,el('p',{text:active?t('progressSafe'):t('homeStory')}),button(active?t('resume'):t('start'),'btn-primary',async()=>{await actions.audio();actions.go(active?(data.activeSession.status==='inspection'?'inspection-request':data.activeSession.status==='victory'?'victory':'mission'):(data.appSettings.showModeSelection?'modes':'intro'),active?{}:{modeId:data.gameModes.find(m=>m.defaultMode)?.id||'normal'})}))))}
