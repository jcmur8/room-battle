import {validPin} from './validation.js';const enc=new TextEncoder();function b64(buf){return btoa(String.fromCharCode(...new Uint8Array(buf)))}export async function hashPin(pin,saltB64=null,iterations=160000){if(!validPin(pin))throw new Error('PIN must have four digits');const salt=saltB64?Uint8Array.from(atob(saltB64),c=>c.charCodeAt(0)):crypto.getRandomValues(new Uint8Array(16));const key=await crypto.subtle.importKey('raw',enc.encode(pin),'PBKDF2',false,['deriveBits']);const bits=await crypto.subtle.deriveBits({name:'PBKDF2',hash:'SHA-256',salt,iterations},key,256);return {salt:b64(salt),hash:b64(bits),iterations}}export async function verifyPin(pin,sec,now=Date.now()){if(sec.lockedUntil&&now<sec.lockedUntil)return {ok:false,locked:true,waitMs:sec.lockedUntil-now};const out=await hashPin(pin,sec.salt,sec.iterations);if(out.hash===sec.hash)return {ok:true,security:{...sec,failedAttempts:0,lockedUntil:0}};const fails=(sec.failedAttempts||0)+1;return {ok:false,locked:false,security:{...sec,failedAttempts:fails,lockedUntil:fails>=5?now+30000:0}}}export function parentSession(timeoutMs=300000){let until=0;return {open(){until=Date.now()+timeoutMs},touch(){if(until)until=Date.now()+timeoutMs},close(){until=0},active(){return Date.now()<until}}}