export function assignRoles(children,mission,index=0,roleDefs=[]){const active=children.filter(c=>c.active!==false&&!mission.childExclusions?.includes(c.id));const desired=mission.roles?.length?mission.roles:['Finder'];return active.map((child,i)=>{const allowed=desired.filter(r=>!child.roleRestrictions?.includes(r));const forced=roleDefs.find(r=>r.lockedChildId===child.id&&desired.includes(r.name));return {childId:child.id,role:forced?.name||allowed[(i+index)%Math.max(allowed.length,1)]||'Helper'}})}export function fairRotation(children,missions,roleDefs=[]){return missions.map((m,i)=>({missionId:m.id,assignments:assignRoles(children,m,i,roleDefs)}))}