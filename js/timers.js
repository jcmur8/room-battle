export function elapsedMs(s,now=Date.now()){if(!s)return 0;let e=(s.accumulatedMs||0);if(!s.pausedAt&&s.runningSince)e+=Math.max(0,now-s.runningSince);return e}export function pauseTimer(s,now=Date.now()){if(s.pausedAt)return s;return {...s,accumulatedMs:elapsedMs(s,now),runningSince:null,pausedAt:now}}export function resumeTimer(s,now=Date.now()){if(!s.pausedAt)return s;return {...s,runningSince:now,pausedAt:null}}export function formatDuration(ms){const sec=Math.floor(ms/1000),m=Math.floor(sec/60),s=sec%60;return `${m}:${String(s).padStart(2,'0')}`}