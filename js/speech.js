export function speak(text,enabled=true,language='en'){if(!enabled||!('speechSynthesis'in window)||!text)return false;try{speechSynthesis.cancel();const u=new SpeechSynthesisUtterance(String(text));u.rate=.92;u.pitch=1.08;u.lang=language==='es'?'es-US':'en-US';const voices=speechSynthesis.getVoices();const preferred=voices.find(v=>v.lang?.toLowerCase().startsWith(language==='es'?'es':'en'));if(preferred)u.voice=preferred;speechSynthesis.speak(u);return true}catch{return false}}export function stopSpeech(){try{speechSynthesis.cancel()}catch{}}
