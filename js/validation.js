import {SCHEMA_VERSION} from './defaults.js';export function validateRoot(d){const e=[];if(!d||typeof d!=='object')return ['Backup must contain an object.'];if(typeof d.schemaVersion!=='number'||d.schemaVersion>SCHEMA_VERSION)e.push('Unsupported schema version.');for(const k of ['appSettings','parentSecurity','children','roles','zones','missions','gameModes','rewards','sessionHistory'])if(!(k in d))e.push('Missing '+k+'.');if(!Array.isArray(d.children)||d.children.length>8)e.push('Children must be an array of up to eight profiles.');if(!Array.isArray(d.missions))e.push('Missions must be an array.');return e}export function validPin(pin){return /^\d{4}$/.test(pin)}export function safeText(s,max=200){return String(s??'').trim().slice(0,max)}