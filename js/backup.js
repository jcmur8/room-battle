import {validateRoot} from './validation.js';import {migrate} from './migrations.js';export function exportBlob(data){return new Blob([JSON.stringify(data,null,2)],{type:'application/json'})}export function downloadBackup(data,name='room-monster-backup.json'){const u=URL.createObjectURL(exportBlob(data));const a=document.createElement('a');a.href=u;a.download=name;a.click();setTimeout(()=>URL.revokeObjectURL(u),1000)}export async function parseBackup(file){if(file.size>2_000_000)throw new Error('Backup is too large.');if(!file.name.toLowerCase().endsWith('.json'))throw new Error('Choose a JSON backup file.');const data=JSON.parse(await file.text());const migrated=migrate(data);const errors=validateRoot(migrated);if(errors.length)throw new Error(errors.join(' '));return migrated}